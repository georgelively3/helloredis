spring:
  application:
    name: helloredis
  data:
    redis:
      # Used for local dev / Testcontainers when aws.elasticache.enabled=false
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

aws:
  elasticache:
    # Set ELASTICACHE_ENABLED=true to connect to AWS ElastiCache instead of localhost
    enabled: ${ELASTICACHE_ENABLED:false}
    cluster-endpoint: ${ELASTICACHE_CLUSTER_ENDPOINT:}   # e.g. clustercfg.my-cluster.abc123.use1.cache.amazonaws.com
    cluster-name: ${ELASTICACHE_CLUSTER_NAME:}            # e.g. my-cluster-us-east-1 (used for IAM token)
    iam-username: ${ELASTICACHE_IAM_USERNAME:}
    region: ${AWS_REGION:us-east-1}
    port: ${ELASTICACHE_PORT:6389}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs
